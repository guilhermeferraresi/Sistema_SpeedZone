<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Speed Zone - Pagamento PIX</title>
    <link rel="stylesheet" href="/css/pix.css" />
</head>
<body class="pix-page">
    <img src="/img/logo.png" alt="Speed Zone" class="pix-card__logo" />
    <main class="pix-card">
        <header class="pix-card__header">
            <span class="pix-card__heading">FORMA DE PAGAMENTO</span>
        </header>

        <div class="pix-card__grid">
            <div class="pix-card__qr">
                <img src="/img/qrcode.png" alt="QR Code para pagamento por PIX" />
            </div>

            <div class="pix-card__info">
                <div>
                    <span class="pix-label">PIX</span>
                </div>
                <dl class="pix-details">
                    <div>
                        <dt>TOTAL</dt>
                        <dd>R$ 8.812.187</dd>
                    </div>
                    <div>
                        <dd>SpeedZone Inc</dd>
                    </div>
                </dl>
                <a class="pix-btn" asp-controller="Pedido" asp-action="Compra">VOLTAR</a>
            </div>
        </div>
    </main>

    <p class="pix-tagline">Se puder ser comparado, não é nosso</p>
</body>
</html>